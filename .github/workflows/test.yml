name: test

on:
  push:
  pull_request:

jobs:
  structural:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run structural and disposable fixture tests
        run: tests/structural.sh
